#include <bits/stdc++.h>

#define MAX 1012345

using namespace std;

int main() {

    int n, v[MAX];

    cin >> n;

    for (int i = 0; i < n * 2; i++)
    {
        cin >> v[i];
    }

    /*
     for (int i = 0; i < n * 2; i++)
    {
        cout << v[i];
    }
    */

    return 0;
}