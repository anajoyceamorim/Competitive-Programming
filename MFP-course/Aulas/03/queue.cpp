#include <iostream>
#include <queue>

int main(){
         queue<int> fila;
         
         fila.push(10);
         fila.push(5);
         fila.size();
         fila.pop();
         cout << fila.front() << endl;
         fila.empty();
         fila.clear();
}
